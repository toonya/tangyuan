1. npm install  也许可以用maybe那条替代，而且可能速度更快。
2. npm install grunt-autoprefixer
3. npm install grunt-contrib-jade --save-dev

if need pretty: npm i grunt-prettify --save-dev

4. grunt watch



maybe need to npm install grunt --save-dev